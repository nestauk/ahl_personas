# Backend API URL (defaults to http://localhost:8000 if unset)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: shared API key — must match backend API_KEY when auth is enabled
# NEXT_PUBLIC_API_KEY=your-secret-key
